{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/ttezer/gtfs-jp-monitor/schemas/content.schema.json",
  "title": "gtfs-jp-monitor-content/1",
  "description": "Content signature of a publication's ZIP (feeds/<org_id>/<feed_id>/generations/<uid>/content.json), independent of the analysis key. Equal signatures mean every file holds the same values; see src/gtfs_jp_monitor/signature.py and data-model §4.2.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "zip_sha256", "signature", "files"],
  "properties": {
    "schema": { "const": "gtfs-jp-monitor-content/1" },
    "zip_sha256": { "$ref": "generation.schema.json#/$defs/sha256", "description": "The ZIP bytes the signature was computed from; it counts only when it equals the analysed ZIP." },
    "signature": { "$ref": "generation.schema.json#/$defs/sha256" },
    "fields": {
      "description": "How often selected optional fields are filled (data-model §15): per file, its rows and, per field, the rows with a value (for wheelchair and bike fields, 1 or 2). Null when the ZIP could not be read; absent in records written before it existed.",
      "oneOf": [
        { "type": "null" },
        {
          "type": "object",
          "additionalProperties": {
            "type": "object",
            "additionalProperties": false,
            "required": ["rows", "filled"],
            "properties": {
              "rows": { "type": "integer", "minimum": 0 },
              "filled": { "type": "object", "additionalProperties": { "type": "integer", "minimum": 0 } }
            }
          }
        }
      ]
    },
    "files": {
      "description": "Per ZIP member; null when the ZIP could not be read, so the signature is the ZIP digest.",
      "oneOf": [
        { "type": "null" },
        {
          "type": "object",
          "additionalProperties": {
            "type": "object",
            "additionalProperties": false,
            "required": ["method", "hash"],
            "properties": {
              "method": { "enum": ["canonical_csv", "raw_bytes"] },
              "hash": { "$ref": "generation.schema.json#/$defs/sha256" }
            }
          }
        }
      ]
    }
  }
}
